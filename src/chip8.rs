use crate::cpu::Cpu;
use crate::drivers::display::DisplayDriver;

pub struct Chip8 {
    cpu: Cpu,
    display: DisplayDriver,
}

impl Chip8 {
    pub fn draw() {
        //display.draw(cpu.);
    }
}
